<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>edit</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-800 p-5">
      <h2 class="text-2xl text-white">Edit your post...</h2>
      <form
        action="/edit/<%= post._id%>"
        method="post"
        class="mt-4 text-white flex flex-col py-4"
      >
        <textarea
          placeholder="What's on your mind?"
          name="content"
          class="rounded p-4 mb-4 w-70 text-white"
        >
<%= post.content%></textarea
        >
        <input
          class="p-2 bg-blue-500 rounded w-44"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
